# PRD: 간단한 계산기 (Simple Calculator)

## 1. 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | Simple Calculator |
| 작성일 | 2026-05-07 |
| 버전 | v1.0 |

### 목적
사칙연산(더하기, 빼기, 곱하기, 나누기)을 지원하는 웹 기반 계산기를 개발한다.

---

## 2. 범위

- **포함**: 사칙연산 기능을 갖춘 웹 계산기 (HTML/CSS/JavaScript)
- **제외**: 공학 계산, 단위 변환, 계산 히스토리

---

## 3. 사용자 스토리

| ID | 역할 | 요구 | 목적 |
|----|------|------|------|
| US-01 | 사용자 | 숫자 버튼을 눌러 숫자를 입력한다 | 계산할 값을 입력하기 위해 |
| US-02 | 사용자 | 연산자(+, -, ×, ÷)를 선택한다 | 원하는 연산을 지정하기 위해 |
| US-03 | 사용자 | `=` 버튼을 눌러 결과를 확인한다 | 계산 결과를 얻기 위해 |
| US-04 | 사용자 | `C` 버튼으로 입력을 초기화한다 | 실수한 입력을 지우기 위해 |
| US-05 | 사용자 | 소수점 입력이 가능하다 | 정수가 아닌 값을 계산하기 위해 |

---

## 4. 기능 요구사항

### 4.1 핵심 기능

#### FR-01: 숫자 입력
- 0~9 숫자 버튼 제공
- 소수점(`.`) 버튼 제공
- 한 피연산자에 소수점은 1회만 허용

#### FR-02: 사칙연산
| 연산 | 버튼 | 비고 |
|------|------|------|
| 더하기 | `+` | |
| 빼기 | `-` | |
| 곱하기 | `×` | |
| 나누기 | `÷` | 0으로 나눌 경우 에러 표시 |

#### FR-03: 결과 출력
- `=` 버튼 클릭 시 결과를 디스플레이에 표시
- 결과값이 매우 클 경우 지수 표기법(e) 사용 허용

#### FR-04: 초기화
- `C` 버튼: 전체 입력 초기화 (Clear All)
- `⌫` 버튼: 마지막 입력 문자 1개 삭제 (Backspace)

#### FR-05: 연속 계산
- 결과 출력 후 연산자를 바로 누르면 결과값을 첫 번째 피연산자로 사용

---

## 5. 비기능 요구사항

| 구분 | 요구사항 |
|------|----------|
| 호환성 | 최신 Chrome, Firefox, Edge, Safari 지원 |
| 반응성 | 모바일 화면(320px 이상) 대응 |
| 접근성 | 키보드 입력 지원 (숫자, 연산자, Enter, Backspace) |
| 성능 | 버튼 클릭 후 100ms 이내 화면 반영 |

---

## 6. UI 레이아웃

```
┌─────────────────────┐
│        디스플레이        │  ← 입력값 / 결과 표시
│              0      │
├──────┬──────┬──────┬──────┤
│  C   │  ⌫  │      │  ÷   │
├──────┼──────┼──────┼──────┤
│  7   │  8   │  9   │  ×   │
├──────┼──────┼──────┼──────┤
│  4   │  5   │  6   │  -   │
├──────┼──────┼──────┼──────┤
│  1   │  2   │  3   │  +   │
├──────┴──────┼──────┼──────┤
│      0      │  .   │  =   │
└─────────────┴──────┴──────┘
```

---

## 7. 에러 처리

| 상황 | 표시 메시지 |
|------|------------|
| 0으로 나누기 | `Error` |
| 숫자 없이 `=` 클릭 | 무시 (현재 상태 유지) |
| 소수점 중복 입력 | 무시 |

---

## 8. 기술 스택

| 구분 | 선택 |
|------|------|
| 마크업 | HTML5 |
| 스타일 | CSS3 (Flexbox/Grid) |
| 로직 | Vanilla JavaScript (ES6+) |
| 빌드 도구 | 없음 (단일 파일) |

---

## 9. 파일 구조

```
calculator/
├── PRD.md
├── index.html       ← 마크업 + 진입점
├── style.css        ← 스타일시트
└── calculator.js    ← 계산 로직
```

---

## 10. 완료 기준 (Definition of Done)

- [ ] 사칙연산(+, -, ×, ÷) 정상 동작
- [ ] 0 나누기 에러 처리
- [ ] 소수점 입력 동작
- [ ] C / ⌫ 버튼 동작
- [ ] 연속 계산 동작
- [ ] 키보드 입력 동작
- [ ] 모바일 반응형 레이아웃 확인
- [ ] 주요 브라우저(Chrome, Firefox) 동작 확인
